<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="transport.list">
      <field name="name">transport list</field>
      <field name="model">transport.transport</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="transport.action_window">
      <field name="name">transport window</field>
      <field name="res_model">transport.transport</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="transport.action_server">
      <field name="name">transport server</field>
      <field name="model_id" ref="model_transport_transport"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="transport" id="transport.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="transport.menu_1" parent="transport.menu_root"/>
    <menuitem name="Menu 2" id="transport.menu_2" parent="transport.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="transport.menu_1_list" parent="transport.menu_1"
              action="transport.action_window"/>
    <menuitem name="Server to list" id="transport" parent="transport.menu_2"
              action="transport.action_server"/>
    -->
  </data>
</odoo>